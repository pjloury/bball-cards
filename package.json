{
  "name": "hoops-elite",
  "version": "2.0.0",
  "description": "Free NBA Top Shot — a daily card-collecting game. Static SPA + baked ESPN data.",
  "type": "commonjs",
  "scripts": {
    "start": "node scripts/dev-server.js",
    "dev": "node scripts/dev-server.js",
    "build:data": "node scripts/build-data.js",
    "build:photos": "node scripts/fetch-photos.js",
    "build:photos:refresh": "node scripts/fetch-photos.js --refresh",
    "build": "node scripts/build-data.js && node scripts/fetch-photos.js"
  },
  "license": "MIT"
}
